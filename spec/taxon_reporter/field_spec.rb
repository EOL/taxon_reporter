require_relative '../spec_helper'

describe TaxonReporter::Field do
  let(:source) { 'Test' }
  let(:name) { 'Name' }
  subject(:field) { TaxonReporter::Field.new(source, name) }
  
  pending "need to write some tests"
end
